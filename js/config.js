
const config = {
    toast: "top",       // Set to "top" or "bottom" to change the position of the toast message.

    // Start of product configuration
    products: [
        {
            name: "MyCalculator",
            publicKey: "MOBILE_APP_PUBLIC_KEY",
            device: "pixel7"
        }
    ],
    // End of product configuration.
};